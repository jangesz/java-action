前面提到，虽然熔断器的加入能够让服务消费端处理超时失败，但如果服务宕机了呢？

当出现服务宕机，故障转移策略就可以让服务重新启动并工作。

如果要做到故障转移，首先我们必须知道服务的状态是否健康（是否已经宕机等）

健康检查就是用来做这个事情的，健康检查API能够让微服务显示它自身的状态。它能告诉服务调用方当前服务是否正常。

需要知道的是，即使是调用一个健康的服务提供方的服务，也不能保证100%调用成功。健康检查仅仅只是显示微服务是否正在运行。